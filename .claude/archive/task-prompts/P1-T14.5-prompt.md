## 事前にコンテキスト把握（必須）
@.claude/CLAUDE.md @.claude/TASKS.md を読んでください。

## 背景
パッケージスコープ名 `@war-of-dots` は既存ゲーム「War of Dots」の著作権・商標を侵害する可能性がある。
GitHub リポジトリ名 `dots-war-sim` に合わせ、全パッケージスコープを `@dots-war-sim` に統一する。

## 現在の状態
- P1-T1〜T14 全完了。テスト 132 件全 green。
- パッケージ: `@war-of-dots/core`, `@war-of-dots/web`, `@war-of-dots/maps`
- ルート `package.json` の name: `war-of-dots`
- ドキュメント（`.claude/` 配下）にも `@war-of-dots` / `war-of-dots` の参照多数

## 着手前に必ず行うこと（影響範囲調査）

以下のコマンドで影響範囲を洗い出し、変更対象ファイル一覧を提示してから着手すること:

```bash
# ソースコード・設定ファイル内の参照
grep -r "war-of-dots" --include="*.ts" --include="*.json" --include="*.yaml" --include="*.svelte" --include="*.js" .

# ドキュメント内の参照（.claude/ 配下含む）
grep -r "war-of-dots" --include="*.md" .

# @war-of-dots スコープの import
grep -r "@war-of-dots/" --include="*.ts" --include="*.svelte" .
```

調査結果を以下に分類して提示:
1. **必須変更**: package.json, import パス, pnpm-workspace 関連
2. **ドキュメント更新**: CLAUDE.md, TASKS.md, DESIGN.md, SETUP.md 等の参照
3. **変更不要**: changelog / completed-tasks.md 等の過去履歴（事実の記録なのでそのまま）

承認を得てから実装に進むこと。

## 作業内容

### 1. package.json の name 置換
- `war-of-dots` (ルート) → `dots-war-sim`
- `@war-of-dots/core` → `@dots-war-sim/core`
- `@war-of-dots/web` → `@dots-war-sim/web`
- `@war-of-dots/maps` → `@dots-war-sim/maps`
- workspace 依存の `@war-of-dots/*` → `@dots-war-sim/*`

### 2. import パス置換
- 全 `.ts` / `.svelte` ファイル内の `from '@war-of-dots/core'` → `from '@dots-war-sim/core'`
- `from '@war-of-dots/maps'` → `from '@dots-war-sim/maps'`

### 3. ドキュメント更新
- CLAUDE.md: スコープ名、`pnpm --filter` の例示
- TASKS.md: パッケージ参照
- DESIGN.md: モジュール名
- SETUP.md: コマンド例
- P1-T15-prompt.md: パッケージ参照
- **変更しない**: `archive/completed-tasks.md`, `archive/changelog.md` の過去履歴記述

### 4. lockfile 再生成
```bash
pnpm install
```

### 5. 検証
```bash
pnpm -r typecheck
pnpm -r test
pnpm lint
pnpm --filter @dots-war-sim/web dev  # ブラウザで動作確認
```

## 禁止事項
* ゲームロジック（`packages/core/src/sim/`）の動作を変更する
* テストの期待値や振る舞いを変更する（import パスのみ変更）
* 既存テスト 132 件を壊す
* 新規パッケージを追加する

## 受け入れ基準
- [ ] `grep -r "war-of-dots"` がソース・設定から 0 件（過去履歴の記述は除く）
- [ ] `pnpm install` 成功
- [ ] `pnpm -r typecheck` 通る
- [ ] `pnpm -r test` 通る（全 132 件 green）
- [ ] `pnpm lint` 通る
- [ ] `pnpm --filter @dots-war-sim/web dev` でゲームが動作する

## 完了後のコミット
```
chore: rename package scope from @war-of-dots to @dots-war-sim (P1-T14.5)
```

コミット前に `pnpm -r typecheck && pnpm -r test && pnpm lint` を必ず通すこと。
コミット後に `git push` すること。
